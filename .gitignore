# Xcode / SwiftPM
DerivedData/
build/
.build/
.swiftpm/
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# Read-only reference clone of the Windows app (its own git repo — re-clone
# from github.com/Armadillon44/shotAI; see CLAUDE.md).
shotAI-original/

# Entra SSO config baked into the build (#69). Not credentials, but they name a
# tenant and an Anthropic org, and this repo is public — so they ship in the
# binary, never in source. See shotAI/Resources/Federation.example.plist.
shotAI/Resources/Federation.plist

# LFI design system — 9.6MB vendored reference (node_modules-scale dist/), not a build input
design/lfi-design-system/

# Coverage artifact dropped in the CWD by `swift test` / `swift run`.
default.profraw
